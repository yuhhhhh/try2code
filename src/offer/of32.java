package offer;

import java.util.*;

public class of32 {
    public static void main(String[] args) {
        Map<TreeNode, Boolean> stMap = new HashMap<>();
        Deque<TreeNode> deque = new LinkedList<>();
    }

    public static class TreeNode {
        int val;
        TreeNode left;
        TreeNode right;
        TreeNode(int x) {
            val = x;
        }
    }
}
